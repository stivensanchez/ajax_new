<%= form_for [@house, @material], remote: true do |f| %>
  <% if @material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@material.errors.count, "error") %> prohibited this material from being saved:</h2>

      <ul>
      <% @material.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :house_id %><br>
    <%= f.text_field :house_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
    <%= link_to 'Cancelar', house_materials_path(@house)%>
  </div>
<% end %>
